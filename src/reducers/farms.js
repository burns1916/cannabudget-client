import {
    ADD_FARM,
    DELETE_FARM,
} from '../actionTypes'

initialState = {

}

export function farmsReducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
} 